export default function Player(name, result) {
    this.name = name
    // this.result = this.check_result(result)
  }

  // Player.prototype.check_result = function(result){
  //   var final_result = ""
  //
  //   if(result === "Player X win. Player O lose" && this.name === "X") {
  //     final_result = "Player X win"
  //   } else if (result === "Player O win. Player X lose" && this.name === "O") {
  //     final_result = "Player O win"
  //   }
  //   return final_result
  // }
